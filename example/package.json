{
  "scripts": {
    "postinstall": "haxelib newrepo && haxelib install --always install.hxml && haxelib dev react-css ..",
    "build": "haxe build.hxml",
    "start": "parcel bin/index.html"
  },
  "dependencies": {
    "react": "16.14.0",
    "react-dom": "16.14.0"
  },
  "devDependencies": {
    "parcel": "2.0.0-beta.2"
  }
}
